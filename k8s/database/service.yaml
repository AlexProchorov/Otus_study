apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: otus-project-app
  labels:
    app: postgres-pvc
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30004
  selector:
    app: postgresdb







