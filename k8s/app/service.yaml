apiVersion: v1
kind: Service
metadata:
  name: otus-project-app
  namespace: otus-project-app
spec:
  type: ClusterIP
  selector:
    app: otus-project-app
    env: prod
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000



